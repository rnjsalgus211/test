git hub 명령 프롬포트에서 업로드 하는 방법

1. d드라이브에 폴더 생성

2. cmd 창에서

d: 입력

git init     ->초기화 

git add yedam.txt ( or *)

git config --global user.email "이메일@주소"

git config --global user.name "닉네임"

git config --list

git commit -m 'test' (m은 메세지)

